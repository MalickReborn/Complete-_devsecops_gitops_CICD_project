pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                // Checkout the SCM repository where the Jenkinsfile is stored
                checkout scm
            }
        }

    }
